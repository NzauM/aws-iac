version: 0.2

phases:
  install:
    commands:
      - npm ci
  build:
    commands:
      - npm run build
      - npx cdk synth CapstoneStack --output cdk.out
artifacts:
  files:
    - '**/*'
  base-directory: cdk.out